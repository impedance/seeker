# План разработки приложения для просмотра БД ГЭСН

Этот документ содержит пошаговый план разработки веб-приложения для навигации по XML базам данных ГЭСН через BaseX.

---

## Фаза 1: Подготовка окружения

### ☐ t1. Настройка BaseX сервера

**Описание:** Установить и запустить BaseX HTTP сервер для доступа к XML базам через REST API.

**Шаги:**
1. BaseX уже скачан в папку `basex/`
2. Создать базы данных из XML файлов
3. Запустить HTTP сервер BaseX
4. Проверить доступность через `http://localhost:8080`

**Команды:**
```bash
cd basex
# Создание баз данных
./bin/basex -c "CREATE DB gesn ../ГЭСН.xml"
./bin/basex -c "CREATE DB gesnm ../ГЭСНм.xml"
./bin/basex -c "CREATE DB gesnmr ../ГЭСНмр.xml"
./bin/basex -c "CREATE DB gesnp ../ГЭСНп.xml"
./bin/basex -c "CREATE DB gesnr ../ГЭСНр.xml"
./bin/basex -c "CREATE DB fsbts_mat ../ФСБЦ_Мат&Оборуд.xml"
./bin/basex -c "CREATE DB fsbts_mash ../ФСБЦ_Маш.xml"

# Запуск HTTP сервера
./bin/basexhttp
```

**Результат:** BaseX сервер работает на порту 8080, все базы созданы.

**Проверка:** Открыть `http://localhost:8080` в браузере - должна открыться страница BaseX.

---

### ☐ t2. Создание структуры проекта

**Описание:** Создать файловую структуру для веб-приложения.

**Шаги:**
1. Создать папку `app/` в корне проекта
2. Внутри создать подпапки для организации кода
3. Создать пустые файлы для будущей разработки

**Команды:**
```bash
mkdir -p app/{css,js,assets}
touch app/index.html
touch app/css/style.css
touch app/js/config.js
touch app/js/api.js
touch app/js/navigation.js
touch app/js/ui.js
touch app/js/search.js
touch app/js/main.js
```

**Структура:**
```
app/
├── index.html           # Главная страница
├── css/
│   └── style.css       # Кастомные стили
├── js/
│   ├── config.js       # Конфигурация (URL BaseX, названия БД)
│   ├── api.js          # Функции для работы с BaseX REST API
│   ├── navigation.js   # Логика навигации по дереву
│   ├── ui.js           # Отрисовка интерфейса
│   ├── search.js       # Функционал поиска
│   └── main.js         # Точка входа, инициализация
└── assets/             # Картинки, иконки
```

**Результат:** Создана файловая структура проекта.

**Проверка:** Выполнить `ls -R app/` - должны быть видны все папки и файлы.

---

## Фаза 2: Базовая HTML разметка

### ☐ t3. Создание HTML структуры с Bootstrap

**Описание:** Создать HTML разметку главной страницы с подключением Bootstrap 5 и разделением на области.

**Файл:** `app/index.html`

**Что нужно сделать:**
1. Создать базовый HTML5 документ с кодировкой UTF-8
2. Подключить Bootstrap 5 через CDN
3. Подключить Bootstrap Icons для иконок
4. Создать Layout с тремя колонками:
   - Левая панель (навигация по разделам)
   - Центральная панель (дерево элементов)
   - Правая панель (детали выбранного элемента)
5. Добавить header с поиском
6. Подключить все JS модули в правильном порядке

**Ключевые элементы:**
- `<div id="sections-list">` - список разделов/баз
- `<div id="tree-view">` - дерево элементов
- `<div id="details-panel">` - детали выбранного элемента
- `<input id="search-input">` - поле поиска
- Spinner/загрузка для асинхронных операций

**Результат:** HTML файл с полной разметкой интерфейса.

**Проверка:** Открыть `app/index.html` в браузере - должен быть виден интерфейс с тремя панелями.

---

### ☐ t4. Добавление CSS стилей

**Описание:** Написать кастомные стили для улучшения внешнего вида приложения.

**Файл:** `app/css/style.css`

**Что нужно сделать:**
1. Настроить высоту панелей (100vh с учетом header)
2. Добавить скроллинг для каждой панели отдельно
3. Стилизовать элементы дерева (отступы, иконки раскрытия)
4. Добавить hover эффекты для кликабельных элементов
5. Стилизовать панель деталей (карточки, таблицы ресурсов)
6. Адаптивность для мобильных устройств
7. Стили для состояний загрузки

**Ключевые стили:**
- `.tree-item` - элемент дерева с отступами
- `.tree-item.expandable` - элемент с детьми (иконка +/-)
- `.tree-item.active` - выбранный элемент
- `.resource-table` - таблица ресурсов
- `.panel-scroll` - прокручиваемая панель

**Результат:** Красивый и удобный интерфейс.

**Проверка:** Открыть страницу - все элементы должны быть стилизованы и выглядеть аккуратно.

---

## Фаза 3: JavaScript модули

### ☐ t5. Создание модуля конфигурации

**Описание:** Создать файл с настройками приложения (URL BaseX, названия баз данных).

**Файл:** `app/js/config.js`

**Что нужно сделать:**
1. Создать объект `CONFIG` с настройками
2. Указать базовый URL BaseX сервера
3. Создать список баз данных с метаданными
4. Добавить настройки для запросов (таймауты, лимиты)

**Содержимое:**
```javascript
const CONFIG = {
  baseURL: 'http://localhost:8080/rest',
  databases: [
    { id: 'gesn', name: 'ГЭСН - Строительные работы', file: 'ГЭСН.xml' },
    { id: 'gesnm', name: 'ГЭСНм - Монтаж оборудования', file: 'ГЭСНм.xml' },
    { id: 'gesnmr', name: 'ГЭСНмр - Монтаж/демонтаж', file: 'ГЭСНмр.xml' },
    { id: 'gesnp', name: 'ГЭСНп - Промышленное строительство', file: 'ГЭСНп.xml' },
    { id: 'gesnr', name: 'ГЭСНр - Ремонт', file: 'ГЭСНр.xml' },
    { id: 'fsbts_mat', name: 'ФСБЦ - Материалы', file: 'ФСБЦ_Мат&Оборуд.xml' },
    { id: 'fsbts_mash', name: 'ФСБЦ - Машины', file: 'ФСБЦ_Маш.xml' }
  ],
  requestTimeout: 30000,
  pageSize: 100
};
```

**Результат:** Файл конфигурации готов к использованию.

**Проверка:** Проверить что файл содержит корректные настройки.

---

### ☐ t6. Создание модуля API для работы с BaseX

**Описание:** Написать функции для выполнения XQuery запросов к BaseX через REST API.

**Файл:** `app/js/api.js`

**Что нужно сделать:**
1. Создать функцию `executeQuery(database, xquery)` для выполнения XQuery
2. Обработка ошибок и таймаутов
3. Функция `getSections(database)` - получить список разделов первого уровня
4. Функция `getChildren(database, code)` - получить дочерние элементы по коду
5. Функция `getWorkDetails(database, code)` - получить полную информацию о работе
6. Функция `getResourcePrice(resourceCode)` - найти цену ресурса в справочниках
7. Функция `search(query)` - поиск по всем базам

**Ключевые XQuery примеры:**

Получить разделы первого уровня:
```xquery
//Section[@Type='Сборник']
```

Получить Work по коду:
```xquery
//Work[@Code='01-01-001-01']
```

Найти ресурс в справочнике:
```xquery
//Resource[@Code='91.01.01-035']
```

**Обработка ответов:**
- Парсинг XML ответа от BaseX
- Преобразование в JavaScript объекты
- Обработка ошибок (404, timeout, parse errors)

**Результат:** Модуль API с функциями для работы с BaseX.

**Проверка:** Вызвать функцию в консоли браузера - должны вернуться данные.

---

### ☐ t7. Создание модуля навигации

**Описание:** Реализовать логику навигации по иерархическому дереву элементов.

**Файл:** `app/js/navigation.js`

**Что нужно сделать:**
1. Класс или объект `Navigation` для управления состоянием
2. Отслеживание текущей выбранной базы данных
3. Отслеживание текущего пути в дереве (breadcrumbs)
4. Функция `loadSections()` - загрузка разделов в левую панель
5. Функция `expandNode(code)` - раскрытие узла дерева
6. Функция `selectItem(code)` - выбор элемента и показ деталей
7. История навигации (back/forward)
8. Кэширование загруженных данных

**Структура данных:**
```javascript
{
  currentDatabase: 'gesn',
  currentPath: ['01', '1', '1.1'],
  selectedItem: '01-01-001-01',
  expandedNodes: Set(['01', '01.1']),
  cache: Map()
}
```

**Результат:** Модуль для управления навигацией.

**Проверка:** При клике на элемент должно обновляться дерево и детали.

---

### ☐ t8. Создание модуля UI (отрисовка интерфейса)

**Описание:** Функции для отрисовки элементов интерфейса (списки, дерево, детали).

**Файл:** `app/js/ui.js`

**Что нужно сделать:**
1. Функция `renderDatabaseList()` - отрисовка списка баз в левой панели
2. Функция `renderTree(sections)` - отрисовка дерева разделов
3. Функция `renderTreeNode(node, level)` - отрисовка одного узла с отступом
4. Функция `renderWorkDetails(work)` - отрисовка деталей работы
5. Функция `renderResourcesTable(resources)` - таблица ресурсов
6. Функция `renderPriceInfo(resource, price)` - информация о цене ресурса
7. Функция `showLoading(element)` / `hideLoading(element)` - индикаторы загрузки
8. Функция `showError(message)` - показ ошибок пользователю
9. Функция `renderBreadcrumbs(path)` - хлебные крошки

**HTML шаблоны для элементов:**

Узел дерева:
```html
<div class="tree-item" data-code="01" data-level="0">
  <span class="expand-icon">▶</span>
  <span class="item-name">Земляные работы</span>
  <span class="item-code">[01]</span>
</div>
```

Карточка Work:
```html
<div class="card">
  <div class="card-header">
    <h5>Код: 01-01-001-01</h5>
    <p>Название работы</p>
  </div>
  <div class="card-body">
    <table class="table resource-table">...</table>
  </div>
</div>
```

**Результат:** Модуль для отрисовки всех элементов UI.

**Проверка:** Вызвать функции рендеринга - элементы должны появиться на странице.

---

### ☐ t9. Создание модуля поиска

**Описание:** Реализовать функционал поиска по кодам, названиям и ресурсам.

**Файл:** `app/js/search.js`

**Что нужно сделать:**
1. Функция `search(query)` - поиск по запросу
2. Поиск по коду Work (точное совпадение)
3. Поиск по названию (contains text)
4. Поиск по ресурсам
5. Функция `renderSearchResults(results)` - отображение результатов
6. Группировка результатов по базам данных
7. Debounce для поискового ввода (не запрашивать на каждую букву)
8. Подсветка найденного текста

**XQuery для поиска:**
```xquery
// Поиск Work по коду
//Work[contains(@Code, '01-01')]

// Поиск по названию
//Work[contains(@EndName, 'экскаватор')]

// Поиск по ресурсу
//Work[Resources/Resource[@Code='91.01.01-035']]
```

**Результат:** Модуль поиска с различными фильтрами.

**Проверка:** Ввод текста в поиск должен показывать релевантные результаты.

---

### ☐ t10. Создание главного модуля (main.js)

**Описание:** Точка входа приложения, инициализация и связывание всех модулей.

**Файл:** `app/js/main.js`

**Что нужно сделать:**
1. Функция `init()` которая вызывается при загрузке страницы
2. Проверка доступности BaseX сервера
3. Инициализация всех модулей (API, Navigation, UI, Search)
4. Настройка обработчиков событий:
   - Клик на базу данных
   - Клик на узел дерева
   - Клик на Work элемент
   - Ввод в поле поиска
   - Клик на expand/collapse
5. Загрузка начального состояния (первая база, первый раздел)
6. Обработка ошибок и переподключение к BaseX

**Структура:**
```javascript
document.addEventListener('DOMContentLoaded', async () => {
  try {
    await init();
  } catch (error) {
    UI.showError('Не удалось подключиться к BaseX');
  }
});

async function init() {
  // 1. Проверить BaseX
  // 2. Загрузить список баз
  // 3. Загрузить первую базу
  // 4. Настроить обработчики событий
  // 5. Готово
}
```

**Результат:** Приложение полностью инициализируется и готово к работе.

**Проверка:** Открыть страницу - должны загрузиться разделы первой базы.

---

## Фаза 4: Функциональность связей между базами

### ☐ t11. Реализация отображения связанных ресурсов

**Описание:** При выборе Work показывать цены и детали ресурсов из справочников ФСБЦ.

**Файлы:** `app/js/api.js`, `app/js/ui.js`

**Что нужно сделать:**
1. В функции `getWorkDetails()` для каждого ресурса:
   - Определить тип ресурса по коду (91.* = машины, 01.* = материалы)
   - Выполнить запрос в соответствующую базу ФСБЦ
   - Получить цену и характеристики
2. Добавить в `renderResourcesTable()` колонки:
   - Код ресурса
   - Название (из справочника)
   - Количество
   - Единица измерения
   - Цена (из справочника)
   - Стоимость (количество × цена)
3. Обработка случая когда ресурс не найден в справочнике
4. Подсветка ресурсов с ценами (зеленый) и без цен (красный)
5. Клик на ресурс открывает детали из справочника

**XQuery для получения цены:**
```xquery
// В базе fsbts_mash
//Resource[@Code='91.01.01-035']/Prices/Price/@Cost

// В базе fsbts_mat
//Resource[@Code='01.1.01.04-1018']/Prices/Price/@Cost
```

**Результат:** В таблице ресурсов показываются цены из справочников.

**Проверка:** Выбрать Work с ресурсами - должны подтянуться цены.

---

### ☐ t12. Добавление кликабельных ссылок на ресурсы

**Описание:** Сделать коды ресурсов кликабельными для перехода в справочник.

**Файл:** `app/js/ui.js`

**Что нужно сделать:**
1. В таблице ресурсов код ресурса сделать ссылкой
2. При клике на ссылку:
   - Определить базу справочника (91.* → fsbts_mash, 01.* → fsbts_mat)
   - Переключиться на эту базу в левой панели
   - Найти и выделить ресурс в дереве
   - Показать детали ресурса в правой панели
3. Добавить кнопку "Назад" для возврата к предыдущему элементу
4. История переходов (можно использовать history API браузера)

**UI элементы:**
- Ссылка: `<a href="#" data-resource="91.01.01-035">91.01.01-035</a>`
- Кнопка назад: `<button id="back-btn">← Назад</button>`

**Результат:** Навигация между Work и их ресурсами.

**Проверка:** Клик на код ресурса открывает детали из справочника.

---

## Фаза 5: Расширенный функционал

### ☐ t13. Реализация breadcrumbs (хлебные крошки)

**Описание:** Показывать текущий путь навигации в виде кликабельных хлебных крошек.

**Файл:** `app/js/ui.js`, `app/js/navigation.js`

**Что нужно сделать:**
1. Добавить `<nav id="breadcrumbs">` в HTML
2. Функция `updateBreadcrumbs(path)` обновляет хлебные крошки
3. Путь формата: `ГЭСН → 01. Земляные работы → 1. Механизированная разработка → ...`
4. Каждый элемент кликабелен (возврат на уровень выше)
5. Последний элемент не является ссылкой (текущий)

**HTML структура:**
```html
<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="#">ГЭСН</a></li>
    <li class="breadcrumb-item"><a href="#">01. Земляные работы</a></li>
    <li class="breadcrumb-item active">1. Механизированная разработка</li>
  </ol>
</nav>
```

**Результат:** Хлебные крошки показывают текущий путь.

**Проверка:** При навигации хлебные крошки обновляются, клик работает.

---

### ☐ t14. Добавление фильтрации дерева

**Описание:** Возможность фильтровать дерево по типу элементов (Сборник, Раздел, Таблица, Work).

**Файл:** `app/js/search.js`, `app/js/ui.js`

**Что нужно сделать:**
1. Добавить чекбоксы для фильтрации по типам
2. Функция `filterTree(types)` скрывает/показывает элементы
3. При изменении фильтра обновлять видимость узлов
4. Сохранять состояние фильтров в localStorage

**HTML фильтры:**
```html
<div class="filters">
  <label><input type="checkbox" checked value="Сборник"> Сборники</label>
  <label><input type="checkbox" checked value="Раздел"> Разделы</label>
  <label><input type="checkbox" checked value="Таблица"> Таблицы</label>
  <label><input type="checkbox" checked value="Work"> Работы</label>
</div>
```

**Результат:** Фильтрация дерева по типам элементов.

**Проверка:** Снятие галочки скрывает элементы этого типа.

---

### ☐ t15. Добавление экспорта данных

**Описание:** Возможность экспортировать выбранную Work в JSON/CSV.

**Файл:** `app/js/export.js` (новый)

**Что нужно сделать:**
1. Создать новый модуль `export.js`
2. Функция `exportToJSON(work)` - экспорт в JSON
3. Функция `exportToCSV(work)` - экспорт в CSV
4. Кнопка "Экспорт" в панели деталей
5. Формирование файла и скачивание через blob
6. Включение всех данных: Work + ресурсы + цены

**Структура JSON:**
```json
{
  "code": "01-01-001-01",
  "name": "...",
  "measureUnit": "1000 м3",
  "resources": [
    {
      "code": "91.01.01-035",
      "name": "Бульдозеры...",
      "quantity": 1.6,
      "price": 961.73,
      "total": 1538.77
    }
  ]
}
```

**Результат:** Возможность скачать данные в файл.

**Проверка:** Клик на "Экспорт" скачивает файл с данными.

---

### ☐ t16. Добавление избранного

**Описание:** Возможность добавлять Work в избранное для быстрого доступа.

**Файл:** `app/js/favorites.js` (новый)

**Что нужно сделать:**
1. Создать модуль `favorites.js`
2. Хранение избранного в localStorage
3. Кнопка "★" для добавления/удаления из избранного
4. Вкладка "Избранное" в левой панели
5. Показ списка избранных элементов
6. Клик на избранный элемент открывает его детали

**Структура данных:**
```javascript
{
  favorites: [
    { database: 'gesn', code: '01-01-001-01', name: '...' },
    { database: 'gesnm', code: '01-01-001-02', name: '...' }
  ]
}
```

**Результат:** Работает функционал избранного.

**Проверка:** Добавление в избранное сохраняется после перезагрузки.

---

## Фаза 6: Тестирование и оптимизация

### ☐ t17. Тестирование базового функционала

**Описание:** Проверить работу всех основных функций приложения.

**Что проверить:**
1. ✓ BaseX сервер доступен
2. ✓ Загрузка списка баз данных
3. ✓ Переключение между базами
4. ✓ Раскрытие/сворачивание узлов дерева
5. ✓ Выбор Work и отображение деталей
6. ✓ Загрузка цен из справочников
7. ✓ Поиск по коду и названию
8. ✓ Переход по ссылкам на ресурсы
9. ✓ Breadcrumbs работают корректно
10. ✓ Обработка ошибок (нет связи с BaseX)

**Создать тестовые сценарии:**
- Сценарий 1: Найти работу "01-01-001-01" и посмотреть цены ресурсов
- Сценарий 2: Перейти к ресурсу "91.01.01-035" из таблицы ресурсов
- Сценарий 3: Найти все работы с экскаваторами через поиск

**Результат:** Все основные функции работают без ошибок.

**Проверка:** Пройти все тестовые сценарии - не должно быть ошибок.

---

### ☐ t18. Оптимизация производительности

**Описание:** Улучшить скорость работы приложения при работе с большими данными.

**Что оптимизировать:**
1. Кэширование запросов к BaseX (Map с TTL)
2. Виртуальный скроллинг для больших списков (>1000 элементов)
3. Ленивая загрузка детей узлов (только при раскрытии)
4. Debounce для поиска (300ms задержка)
5. Минимизация перерисовок DOM (использовать DocumentFragment)
6. Компрессия больших ответов от BaseX

**Измерения производительности:**
- Время загрузки первого уровня разделов: < 1 сек
- Время раскрытия узла: < 500мс
- Время поиска: < 2 сек
- Потребление памяти: < 100MB

**Инструменты:**
- Chrome DevTools Performance
- Network tab для отслеживания запросов

**Результат:** Приложение работает быстро даже с большими данными.

**Проверка:** Измерить время операций - должны быть в пределах целевых значений.

---

### ☐ t19. Добавление обработки ошибок и валидации

**Описание:** Улучшить обработку ошибок и добавить пользовательские сообщения.

**Что добавить:**
1. Try-catch блоки во всех async функциях
2. Красивые сообщения об ошибках (toast/alert)
3. Автоматическое переподключение к BaseX при разрыве связи
4. Валидация ответов от BaseX (проверка структуры XML)
5. Graceful degradation (работа без части данных)
6. Логирование ошибок в консоль для отладки

**Типы ошибок:**
- Сеть недоступна (Network Error)
- BaseX не отвечает (Timeout)
- База данных не найдена (404)
- Некорректный XQuery (400)
- Ресурс не найден в справочнике

**UI для ошибок:**
```html
<div class="alert alert-warning">
  <strong>Внимание!</strong> Не удалось загрузить цены.
  <button class="btn-retry">Повторить</button>
</div>
```

**Результат:** Все ошибки обрабатываются корректно.

**Проверка:** Выключить BaseX - приложение должно показать ошибку.

---

## Фаза 7: Документация

### ☐ t20. Создание README для приложения

**Описание:** Написать документацию по использованию приложения.

**Файл:** `app/README.md`

**Содержание:**
1. Описание проекта
2. Установка и запуск
3. Структура интерфейса (скриншоты)
4. Основные возможности
5. Как работать с поиском
6. Как использовать избранное и экспорт
7. Решение проблем (troubleshooting)

**Результат:** Документация по использованию приложения.

---

### ☐ t21. Создание документации для разработчиков

**Описание:** Документировать код и архитектуру для будущих разработчиков.

**Файл:** `app/DEVELOPMENT.md`

**Содержание:**
1. Архитектура приложения (диаграмма модулей)
2. Описание каждого модуля (назначение, API)
3. Структура данных
4. XQuery примеры для частых операций
5. Как добавить новую функцию
6. Как дебажить проблемы с BaseX
7. Best practices

**Результат:** Документация для разработчиков.

---

## Готово! 🎉

После выполнения всех задач у вас будет полнофункциональное веб-приложение для навигации по XML базам ГЭСН с поддержкой:
- Навигации по иерархическому дереву
- Поиска по кодам и названиям
- Отображения связей между базами (ресурсы и цены)
- Экспорта данных
- Избранного
- Красивого и удобного интерфейса

**Порядок разработки:**
1. Сначала сделать t1-t10 (базовый функционал)
2. Протестировать что все работает
3. Затем t11-t16 (расширенный функционал)
4. В конце t17-t21 (тестирование и документация)

**Время разработки:**
- Для джуна: 3-5 дней полной работы
- Для миддла: 2-3 дня
- Для сеньора: 1-2 дня
