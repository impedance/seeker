- [x] Поиск: скрывать группы/разделы с 0 результатов в выдаче (не показывать пустые блоки), подтвердить нужное поведение.
- [x] Поиск: автозапускать только по выбору подсказки; по Enter/кнопке «Найти» выполнять поиск по введенной фразе.
- [ ] Подсказки: показывать только «Раздел — следующий подраздел» (например, «Конвейеры — Оборудование швейных...») вместо полного пути.
- [ ] Поиск: при клике на результат (панель справа) принудительно скрывать выпадающие подсказки, потому что сейчас слой остается видимым и мешает обзору. Файлы: `app/js/main.js`, `app/js/ui.js`, `app/js/search.js`. Feedback loop: после правки убедиться, что поиск работает (ввод → Enter/клик → результат), и список подсказок исчезает сразу после клика без сдвига дерева.
- [ ] Поиск: добавить кнопку «×» (с зачисткой поля) внутри строки поиска, чтобы пользователь мог быстро стереть запрос. Файлы: `app/index.html`, `app/css/style.css`, `app/js/search.js`. Feedback loop: ввести текст, нажать крестик, убедиться, что поле очищается, подсказки прячутся, а кнопка видна при необходимости.
- [ ] Шапка: выровнять строку поиска по центру заголовка (между навигацией и переключателем темы), чтобы блок выглядел симметрично на широких экранах. Файлы: `app/index.html`, `app/css/style.css`. Feedback loop: открыть страницу, посмотреть хедер на разных ширинах и подтвердить, что search-bar не смещен влево/вправо.
- [ ] Подсказки: разместить блок подсказок строго под строкой поиска и выводить элементы в ряд (не в колонку), чтобы матрица подсказок занимала всю ширину поля и по‑другому не сдвигала соседние элементы. Файлы: `app/css/style.css`, `app/js/ui.js`. Feedback loop: набрать текст ⇒ увидеть группу подсказок в одной строчке под полем, убедиться, что нижние подсказки не накладываются на дерево и что ширина без скролла соответствует ширине ввода.
